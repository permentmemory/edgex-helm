apiVersion: v1
kind: Service
metadata:
  labels:
    org.edgexfoundry.service: {{.Values.edgex.app.appservice.rules}}
  name: {{.Values.edgex.app.appservice.rules}}
  namespace: default
spec:
  ports:
  - name: http-0
    protocol: TCP
    port: 48100
    targetPort: 48100
  selector:
    org.edgexfoundry.service: {{.Values.edgex.app.appservice.rules}}
  clusterIP: None
status:
  loadBalancer: {}
